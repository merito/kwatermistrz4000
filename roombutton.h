#ifndef ROOMBUTTON_H
#define ROOMBUTTON_H

#include <QWidget>

class RoomButton : public QWidget
{
    Q_OBJECT
public:
    explicit RoomButton(QWidget *parent = 0);
    ~RoomButton();

signals:

public slots:
};

#endif // ROOMBUTTON_H
